# Next decision

The appropriate next step is a **three-seed dense replication proposal** of
the fixed family contrasts, not long-context or persistent-state work yet.
The seed-11 result is sufficiently positive and internally coherent to justify
that replication, but it is not confirmatory by itself. The replication should
keep the dense stride-1 cohort, fixed base families, train-only scaling, pooled
validation C selection, and source-disjoint folds unchanged.

Do not execute that replication in this task. If the three-seed result remains
positive, a new preregistered matrix-specific analysis can decide whether the
small conditional C signal merits a larger study. If it disappears, stop the
xLSTM-specific mechanism line rather than moving immediately to W128/W256 or
persistent state.
