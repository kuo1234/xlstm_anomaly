# Phase F-v3 prospective amendment

The bounded Spark/GB10 diagnostic on the exact quarantined
`data/phase_f_v2/runs/lstm_11/best.pt` passed every original frozen gate when
cuDNN was disabled only around matched `nn.LSTM` execution. cuDNN-enabled F-v2
reproduced the known B128/B1 failure. The diagnostic report is
[spark_cudnn_diagnostic.json](../phase_f_v2/spark_cudnn_diagnostic.json); it
uses only the sealed E2 random fixture and no labels, test data, or optimizer
steps. F-v2 remains immutable and its checkpoint remains quarantined.

F-v3 changes exactly one execution detail for the matched LSTM:

```python
with torch.backends.cudnn.flags(
    enabled=False, benchmark=False, deterministic=True, allow_tf32=False
):
    # all six matched nn.LSTM calls for this independent window
```

This scope is local to `MatchedLSTM.forward`; xLSTM retains the accepted F-v2
global backend (`cudnn.allow_tf32=False`, vanilla float32). It is an
implementation-validity correction caused by numerical reference/partition
failures, not by detector, anomaly, validation, or probe performance.

All frozen scientific settings remain unchanged: improved official xLSTMAD
commit `e8b56ba27352733bb83729e85b1d6196dca70c99`, xlstm2.0.5/lightning2.6.1,
D8/W64/embedding40 (73,504 parameters), standard six-layer H38 LSTM (71,790),
train sources1000–1009, validation sources2000–2004, prefix0:4096, sealed
per-source scalers and epoch orders, batch128, Adam 0.001, 50 full epochs,
aligned B,W,D MSE, earliest validation-MSE tie, detector seeds11/22/33/44/55,
fixed tolerances `atol=1e-5, rtol=1e-4`, and no test sources3000–3009.

Every run starts from a fresh initialization. No F-v2 weight/checkpoint is
reused. The xLSTM model and architecture are byte-for-byte the F-v2 path; only
matched LSTM recurrent calls use the scoped cuDNN-disabled context. The fixed
random-unlabeled canary (seed710, shape B128×64×8) runs after every completed
training epoch and compares raw output, reconstruction score, and common18
features against 128 B1 calls with the same frozen tolerance. A canary failure
aborts that run immediately; it never changes optimization, validation
selection, checkpoint selection, or future data.

Before training, `phase_f_v3_gates.py` must pass all existing mechanical gates,
the scoped-LSTM assertion, an untrained canary, and the sealed F-v2 diagnostic
reference. After each run, the complete original post-training parity must pass
for the best checkpoint. Any failure is an implementation STOP; do not relax
tolerances, switch hardware, tune, or inspect anomaly performance. Phase G
remains locked until all ten F-v3 checkpoints and parity reports pass external
review.

No H1/H2/H3 result is generated by this amendment. H1 controlled harm remains
STOP, natural harm NOT_RUN, overall UNRESOLVED; no probes, anomaly AP/AUROC,
H3b, H4, rollback, dual memory or learned gate is authorized.
